{
	"name": "icon-bullet-helper",
	"version": "1.0.1",
	"description": "Theme-independent SVG icon bullets from plain Markdown markers for Obsidian.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production && mkdir -p build && cp main.js manifest.json styles.css build/",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": [
		"obsidian",
		"obsidian-plugin",
		"markdown",
		"svg",
		"bullet"
	],
	"author": "JaewonE",
	"license": "GPL-3.0",
	"devDependencies": {
		"@codemirror/state": "^6.6.0",
		"@codemirror/view": "^6.42.1",
		"@types/node": "^16.11.6",
		"esbuild": "0.25.5",
		"obsidian": "latest",
		"tslib": "2.4.0",
		"typescript": "4.7.4"
	}
}
